<%@subject=MgSubject.where(:is_deleted => '0', :mg_school_id=>session[:current_user_school_id]).pluck(:subject_name, :id)%>

        <%=select_tag :mg_subject_id, options_for_select(@subject),:prompt=>"Select A Subject",:onchange=>"myBatchSubjectsFunction(this.value)" %><br>
      



