<div class="mg-scroll-employee-bar">
<table class="batch-tbl mg-tbl-margin">
<th><%=t('department')%></th>
  <th><%=t('actions')%>
  <div class="mg-text-margin">
        <a class="select-all-department-for-album-non mg-pop-link"><%=t('all')%></a>&nbsp;<a class="select-none-department-for-album-non mg-pop-link"><%=t('none')%></a>
      </div></th>
  	<%@department_list.each do |dept|%>
  	<tr>
  		<td><%=dept.department_name%></td>
  		<td class="mg-align-center">
	  		<%if @department_list_tech_checked.present?%>
		  		<%if @department_list_tech_checked.include? dept.id%>
		  			<%= check_box_tag 'tech_mg_employee_department_id[]', dept.id, true ,{:class=>"class-albumb-department-committee-class-non"}%>
		  		<%else%>
		  			<%= check_box_tag 'tech_mg_employee_department_id[]', dept.id, false ,{:class=>"class-albumb-department-committee-clas-nons"}%>
		  		<%end%>
	  		<%else%>
	  			<%= check_box_tag 'tech_mg_employee_department_id[]', dept.id, false ,{:class=>"class-albumb-department-committee-class-non"}%>
	  		<%end%>
  		</td>
  	</tr>
  	<%end%>
</table>
</div>

<script>
	 $(document).on("click", ".select-all-department-for-album-non", function(e){

    $(".class-albumb-department-committee-class-non").prop('checked', true);
        e.preventDefault();
  });
   $(document).on("click", ".select-none-department-for-album-non", function(e){
    $(".class-albumb-department-committee-class-non").prop('checked', false);
        e.preventDefault();
  });
</script>
