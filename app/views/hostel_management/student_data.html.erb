<%#=select_tag :mg_student_id,options_for_select(@data),{include_blank: false,:prompt=>"Select",:class=> 'mg-text-field student-name'}%>




<% if params[:data].present?%>
	<%=select_tag :mg_student_id,options_for_select(@data,:selected=>@student_name.id),{include_blank: false,:prompt=>"Select",:class=> 'mg-text-field student-name'}%>

	<%else%>
		<%=select_tag :mg_student_id,options_for_select(@data),{include_blank: false,:prompt=>"Select",:class=> 'mg-text-field student-name'}%>

	<%end%>