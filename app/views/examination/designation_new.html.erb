<!-- breadcrumbs Work -->
<% navigation_add 'Examinations', examinations_path %>
<% navigation_add 'Designations', examination_designation_new_path %>

<!-- breadcrumbs Work -->



<%= render  partial: 'examination_table_left' %>
<h4 class="mg-h4">Class Designation</h4>
<div>
<%=hidden_field_tag :mg_course_id,  @Id, :id=>"defaultDesignationId"%>
	<table>
		<tr>
			<td>
				<label class="mg-label mg-leave-width"><%=t('select')%> <%=t('course')%></label>
				<%=select_tag :mg_course_id, options_for_select(@course,:selected=>@Id),:prompt=>t('select'),:onchange=>"myDesignationFunction(this.value)" %><br>
			</td>
		</tr>
	</table>

	




	<div id="examinationDesignationClassID">
	</div>

	<div id="editrankingClassID">
	</div>

	<div id="examinationDesignationID">
	</div>
</div>
</div>
<%= render  partial: 'examination_table_close' %>

<script>

$(document).ready(function(){
		
        focusCurrentTab("viewStudentListAId","viewStudentListLiId");
        focusExaminationHeaderBox();
	var Id=$("#defaultDesignationId").val();
	
	if(Id ==""){
		
		myDesignationFunction(Id);

	}
	if(Id!=""){
		myDesignationFunction(Id);
	}
});

</script>

