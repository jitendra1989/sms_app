
<% unless @students.empty?  %>
  <div class="list_name">
    <%= t("students_in_batch") %>
    <%= image_tag("loader.gif",:align => "absmiddle",:border => 0,:id => "loader", :style =>"display: none;" ) %>
  </div>
  <ul class="student_list">
    <% @students.each do |s| %>
      <li class="student_names">
        <a href="#" id="<%=s.id%>"  class="student-link" > <%= s.first_name %></a>
      </li>
    <% end %>
  </ul>
<% else %>
 <h5> <%= t("no_students_in_batch")  %></h5>
<% end %>