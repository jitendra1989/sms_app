<%@subject=MgSubject.pluck(:subject_name, :id)%>

        <%=select_tag :mg_subject_id, options_for_select(@subject),:prompt=>"Select A Subject",:onchange=>"myEmployeeTopicFunction(this.value)" %><br>
      



