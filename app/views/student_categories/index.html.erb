

<!-- breadcrumbs Work -->
<% navigation_add 'Student Category', student_categories_path %>

<!-- breadcrumbs Work -->


<!-- Sec --> 
<%if @index!="hello"%>
<%#= render  partial: 'students/student_left' %>
<%= render  partial: 'master_settings/master_settings_left' %>
<%end%>
   <input type="button" value="Add New Category" id= "addStudentCategoryBtn" class="mg-custom-form-btn mg-btn-bottom-margin"/>
   <br/>
   <%# if(@mg_student_categories.length > 0) %>   
   <!-- Pie chart starts -->
   <%#  @catList =   @mg_student_categories

      dataHash = Hash.new 


      @catList.each do |catObj|

           @studentSize = MgStudent.where(:mg_student_catagory_id => catObj.id).size
 
          dataHash[catObj.name] = @studentSize
       end 
      

  %>
  <%#= pie_chart(dataHash) %>
   <!-- Pie chart ends -->
   <%#end%>

<% if(@mg_student_categories.length > 0) %>   
    <div class="mg_pagination">
        <h4 class="mg-h4"><%=t('listingcategory')%></h4>

        <table class="batch-tbl" id='people_table'>
            <tr>
              <th><%=t('name')%></th>
              <th><%=t('actions')%></th>
            </tr>

            <% @mg_student_categories.each do |mg_student_category| %>

              <%= render partial: 'table_row', locals: {mg_student_category: mg_student_category} %>
            <% end %>
        </table>
            <br/>

        <%= will_paginate @mg_student_categories, :container => false %>
        <br/>
    </div>
<% else %>
<p class="mg-text-margin">Please create some student category</p>
<% end %>
<div id="createStudentCaregoryDiv"></div>
<div id="editStudentCategoryDiv"></div>
     <%if @index!="hello"%>
<%= render  partial: 'students/student_left_close' %>
<%end%>
<script>
$(document).ready(function () {
  focusCurrentTab("createNewStudentCategoryAID","createNewStudentCategoryLiID");
});

</script>
