<!-- Sec --> 
<% @articles = MgEmployee.all %>

<table class="batch-tbl">
  <tr>
    <th>Employee_Number</th>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Position Id</th>
    <th>Department Id</th>
    <th><%=t("actions")%></th>
    
    
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.employee_number %></td>
      <td><%= article.first_name %></td>
      <td><%= article.last_name %></td>
      <% @temp=article.mg_employee_position_id,%>
<!--<#% @em="select position_name from mg_positions where id=#{@temp}"%>
<#% @em=MgPosition.find_by_sql(@em)  %>
<td> <#%=  @em.position_name   %>  </td>

     
     <td><#%= MgPosition.where(id: article.employee_position_id).position_name %></td>-->
     <td><%= article.mg_employee_position_id %></td>
      <td><%= article.mg_employee_department_id %></td>
      
    
      <td><%= link_to 'Edit', edit_employee_path(article) %> | 
      <%= link_to 'Delete', employee_path(article),
                    method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<!--<td><#%= link_to 'Show', article_path(article) %></td>
    <#%= link_to 'Edit', edit_employees_path(f) %>
    <#%=link_to "Label", :controller => :employees, :action => :edit %>-->

<style>
    

.toggle-data {

    display: block;
    padding: 0 0 10px; 
}


table.batch-tbl {
   /* font-family: verdana,arial,sans-serif;*/
    font-size:11px;
    color:#104564;
    border-width: 1px;
    border-color:  #6592b2;
    border-collapse: collapse;
    margin-left: 20px;
}
table.batch-tbl th {
    background: linear-gradient(to bottom, #d9dfeb, white) repeat scroll 0 0 rgba(0, 0, 0, 0);
    border-width: 1px;
    padding: 8px;
    border-style: solid;
    border-color:  #6592b2;
}
table.batch-tbl tr {
    background-color:white;
}
table.batch-tbl td {
    border-width: 1px;
    padding: 8px;
    border-style: solid;
    border-color:  #6592b2;
}
.anchor-btn{

    text-decoration: none !important;
}

</style>






